-- Stat was a good idea nobody wired up: every public figure with a source and
-- an as-of date. SiteField won that job instead, so this table was empty and
-- referenced by nothing.
DROP TABLE IF EXISTS "Stat";
